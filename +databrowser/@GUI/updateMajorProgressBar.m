function updateMajorProgressBar( self, fraction, varargin )
%UPDATEMAJORPROGRESSBAR  Updates the major progress bar of the splash screen
%
%  self.updateProgressBar( fraction ) 
%      Updates the PB to show fraction (fraction between 0:1)
%
%  self.updateProgressBar( fraction, text )
%      Updates the PB to show fraction (fraction between 0:1) and updates
%      the text shown in the Bar
%
%
